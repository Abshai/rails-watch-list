<%= simple_form_for(@list, @bookmark) do |f| %>
  <%= f.input :comment %>
  <%# f.association creates a dropdown %>
  <%= f.association :movie%>
   <%= f.submit "Bookmark Movie", class: "btn btn-primary" %>
<% end %>
